<!DOCTYPE html>
<html>
    <head>
      <meta charset="UTF-8"/>
      <title>Recuperados da Covid19</title>
    </head>

    <body>
      <h1> Recuperados Covid19 </h1>

      <table>
        <thead>
          <td>País</td>
          <td>Cidade</td>
          <td>Recuperados</td>
          <td>Última Atualização</td>
        </thead>
        <tbody>
          <% @recovered.each do |recovered| %>
            <tr>
              <td><%= recovered["countryRegion"] %></td>
              <td><%= recovered["provinceState"] %></td>
              <td><%= recovered["recovered"] %></td>
              <td><%= Time.at(recovered["lastUpdate"].to_i/1000).strftime('%d/%m/%Y %H:%M:%S') %></td>
            </tr>
          <% end %>

        </tbody>
      </table>

    </body>

</html>