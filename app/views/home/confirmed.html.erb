<!DOCTYPE html>
<html>
    <head>
      <meta charset="UTF-8"/>
      <title>Casos Confirmados da Covid19</title>
    </head>

    <body>
      <h1> Casos Confirmados da Covid19 </h1>

      <table>
        <thead>
          <td>País</td>
          <td>Cidade</td>
          <td>Casos Confirmados</td>
          <td>Última Atualização</td>
        </thead>
        <tbody>
          <% @confirmed.each do |confirmed| %>
            <tr>
              <td><%= confirmed["countryRegion"] %></td>
              <td><%= confirmed["provinceState"] %></td>
              <td><%= confirmed["confirmed"] %></td>
              <td><%= Time.at(confirmed["lastUpdate"].to_i/1000).strftime('%d/%m/%Y %H:%M:%S') %></td>
            </tr>
          <% end %>

        </tbody>
      </table>

    </body>

</html>